spring:
  application:
    name: cdc-api
  cloud:
    config:
      uri: https://cloud-config.tingcore-central.com
#      Uncomment if you wish to fail the app from starting if no Config Server is available
#      fail-fast: true
#      retry:
#        multiplier: 2
#        max-interval: 10000

---
spring:
  profiles: dev
  profiles.include: monitoring

---
spring:
  profiles: test
  profiles.include: monitoring

---
spring:
  profiles: stage
  profiles.include: monitoring

