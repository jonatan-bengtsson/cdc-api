spring:
  application:
    name: cdc-api
  cloud:
    config:
      uri: http://cloud-config-server.tingcore-central.com
#      Uncomment if you wish to fail the app from starting if no Config Server is available
#      fail-fast: true
#      retry:
#        multiplier: 2
#        max-interval: 10000

