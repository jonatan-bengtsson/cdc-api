spring:
  profiles:
    active: it

flyway:
    enabled: true

logging:
  level:
    com:
      tingcore:
        users: TRACE
    root: TRACE

app:
  allowed-origins: http://localhost:3000

security:
  basic:
    enabled: false